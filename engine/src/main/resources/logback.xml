<?xml version="1.0" encoding="UTF-8"?>
<configuration>
 
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- By default, encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
        <!-- 
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread][%taskId] %-5level %logger{5} - %msg%n</pattern>
            <conversionRule conversionWord="taskId" converterClass="com.mypkg.UserIdConverter" />
        </encoder>
        -->
        
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
	      <layout class="net.alteiar.layouts.TaskLayout" />
	    </encoder>
    </appender>
 
 
    <!-- 
    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>-->
    
    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>
 
</configuration>